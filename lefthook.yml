commit-msg:
    commands:
        commitlint:
            run: bun commitlint {1}

pre-commit:
    parallel: true
    commands:
        lint:
            glob:
                - '*.json'
                - '*.ts'
                - '*.tsx'
            run: bun biome check --write {staged_files}
